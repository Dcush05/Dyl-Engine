------ENGINE TODO LIST------


#DONE(Done meaning that there is implementation that can be built upon and is modular):
	Setup better debug and logging system
	simply do more research on batch rendering instead of relying on llm
	3D SUPPORT

#STARTED(Started meaning that the scaffolding is setup but not fully implemented):
	Setup entity system and be able to interface with the system in the editor
	Split editor renderer and scene renderer
	BETTER STRING HANDLING 
	Make renderer better and more compact
		-Add rendering commands plz
		-Clean up batch refactoring code

#TODO LIST:	
	Render through meshes ???
	Render models
		-Have the choice to swithc between instanced rendering or batch rendering when dealing with meshes (some meshes are static so maybe have them batched while
		dynamic meshes should be instanced)
	BETTER OPERATING SYSTEMS ABSTRACTIONS for window and event handling
	Setup assets system
	Refactor so our code has better taste
	Fix camera code so that it is window agnostic or  make an interface for the camera code
	Setup a scene handling system
		different scenes
		each scene can have a different screen??
	Editor Ui implementation
	Configuration file for window settings

